post:
  operationId: post-auth-logout
  summary: ログアウト
  tags: [auth]

  security:
    - bearerAuth: []

  responses:
    "200":
      description: OK

    "403":
      $ref: "../response/Error403.yaml"

    "429":
      $ref: "../response/Error429.yaml"
